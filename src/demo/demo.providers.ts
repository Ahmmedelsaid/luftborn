import { HttpInterceptorFn } from '@angular/common/http';

/**
 * Empty in every normal build.
 *
 * `angular.json`'s `demo` configuration replaces this file with
 * `demo.providers.demo.ts`, so the in-browser stand-in for the API is compiled
 * into the GitHub Pages bundle and into nothing else.
 */
export const DEMO_INTERCEPTORS: HttpInterceptorFn[] = [];
